{% import "form-builder/_includes/_components" as components %}

{% set name = 'notifications[email]['~ item ~'][attachments][enabled]' %}
{% set enabled = attribute(form.notifications.email, item) is defined and attribute(form.notifications.email, item).attachments.enabled ? true : false %}

{% set container = {
    title: 'Attachments' |t,
    info: null,
    icon: null,
    toggle: true,
    class: null,
    id: null
} %}

{% set inputs = {
    0: {
        type: 'checkbox',
        name: name,
        value: '1',
        checked: enabled,
        class: 'hidden',
        id: null
    }
} %}

{% set modal = null %}

{{ components.optionItem(
    container,
    modal,
    inputs
) }}