{% import "form-builder/_includes/_components" as components %}

{% set container = {
    title: 'Input Container Class' |t,
    info: 'Global container class for all inputs.' |t,
    icon: null,
    toggle: false,
    class: null,
    id: null
} %}

{% set inputs = {
    0: {
        type: 'text',
        name: 'settings[fields][globalInputContainerClass]',
        value: form.settings.fields.globalInputContainerClass is defined ? form.settings.fields.globalInputContainerClass : '',
        class: 'hidden',
        id: null,
        hint: 'CLASS'
    }
} %}

{% set modal = {
    title: 'Input Container Class' |t,
    instructions: 'Global container class for all inputs.' |t,
    successMessage: 'Global Input Container Class Updated' |t,
    inputs: inputs
} %}

{{ components.optionItem(
    container,
    modal,
    inputs
) }}